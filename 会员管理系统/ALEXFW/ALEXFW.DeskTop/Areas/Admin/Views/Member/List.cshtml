@model EntityViewModel<ALEXFW.Entity.UserAndRole.Member>
@{
    ViewBag.Title = "会员列表";

    var area = ViewContext.RouteData.DataTokens["Area"];
    if (area == null)
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
    else
    {
        Layout = "~/Areas/" + area + "/Views/Shared/_adminLayout.cshtml";
    }

    var nameOrCode = ViewBag.NameOrCode;
    string partialViewPath = ViewBag.PartialViewPath;
}

<ol class="breadcrumb">
    <li>
        <a href="@Html.Raw(Url.Action("Index", "Home"))">工作桌面</a>
        </li>
    <li class="active">会员列表</li>
</ol>

<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">

            <!-- Default panel contents -->
            <div class="panel-heading">
                &nbsp;
                <a href="@Html.Raw(Url.Action("Create"))" class="btn btn-primary pull-right" style="margin-left: 10px; margin-top: -7px;">新增会员</a>
                @*<a runat="server" id="SearchMember" href="" class="btn btn-success pull-right" style="margin-left: 10px; margin-top: -7px;">查询</a>
                    <input class="pull-right form-control" type="text" id="nameOrPhone" placeholder="工号或登录名" value="@nameOrCode" style="margin-left: 10px; margin-top: -7px; width: 20%;" />*@

            </div>
            @*<div class="panel-body">
                <form id="form" class="form-horizontal" role="form" action="@Url.Action("SearchMember", "member")" method="post">

                    <div class="form-group">
                        <label class="col-sm-2 control-label">查询</label>
                        <div class="col-sm-8">
                            <input class="form-control" id="searchTerms" name="searchTerms" placeholder="用户名或姓名" type="text" value="" />
                            <div id="Error" style="color: red; font-size: 12px;"></div>
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-sm-offset-2 col-sm-10">
                            <input type="submit" class="btn btn-primary" id="submit" value="确定" />
                            <input type="reset" class="btn btn-default" value="取消" />
                        </div>
                    </div>
                </form>
            </div>*@
            <!-- Table -->
            @Html.Partial(partialViewPath, Model)
        </div>

    </div>
</div>


